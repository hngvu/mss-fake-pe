spring:
  application:
    name: brand-service
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
  springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
